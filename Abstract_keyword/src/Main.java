public class Main {

	public static void main(String[] args) {

		// abstract = abstract classes cannot be instantiated, but they can have
		// a subclass abstract method are declare without an implementation

		car car = new car();
		// Vehicle vehicle = new Vehicle();
		car.go();
	}

}
